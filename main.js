function getLines() {
    let line1 = prompt("введіт перший рядок:");
    let line2 = prompt("введіть другий рядок:");
    let line3 = prompt("введіть третій рядок:");

    console.log(`${line1} ${line2} ${line3}`);
}